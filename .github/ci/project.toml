# Generated by velnor-workflow. Regenerate; do not hand-edit.
schema = 3
repository = "tailrocks/termpane"
profile = "generic"
verified = true
default_branch = "main"
providers = ["github-hosted"]
automatic_providers = ["github-hosted"]
default_dispatch_providers = ["github-hosted"]

[analysis]
method = "static-filesystem-and-manifest-inspection"
detected = ["cargo-deny-policy", "cargo-lockfile", "cargo-nextest-policy", "rust-binaries:termpane-fuzz:1", "rust-features:termpane:2", "rust-test-targets:termpane:1", "rust-workspace-members:2"]
limitations = ["Project code, build scripts, task runners, and commands are never executed during analysis.", "Release, signing, registry, deployment, branch-protection, and runner-capability contracts remain explicit manual inputs."]

[workflow]
files = ["ci-main.yml", "ci-policy.yml", "ci-pr.yml", "ci-unit-rust.yml", "maintenance.yml", "nightly.yml"]
notes = ["Rust verification uses Mr. Boxington 1.12.0 on providers [github-hosted]. GitHub-hosted jobs use its GitHub cache backend with bounded snapshot keys (a key names the toolchain/image/linker compatibility class and the hashed source state, so a new source state saves a new snapshot and retention keeps generations bounded). Local-provider jobs use an explicit local-backend setup step against the image/runner-provided local store."]

[release]
enabled = false
reason = "Release is fail-closed. Enable only after declaring immutable artifact, registry, provenance, and tag-protection policy."

[[unit]]
id = "rust-policy"
label = "Rust dependency policy"
kind = "rust"
root = "."
watch = [".cargo/audit.toml", ".cargo/deny.toml", "Cargo.lock", "Cargo.toml", "audit.toml", "deny.toml"]
pr_commands = ["mbx deny check"]
full_commands = ["mbx deny check"]
platform = "linux-x64"
trust = "untrusted-ok"
[unit.capabilities]
docker = true
nested_privileged_docker = false
buildx_compose = false
testcontainers = true
services_with_readiness = false
browser_binaries = false
native_macos_arm64 = false
[unit.cache]
key_files = [".cargo/**", "Cargo.lock", "deny.toml"]
paths = ["~/.cargo/registry", "~/.cargo/git"]

[[unit]]
id = "rust-termpane"
label = "Rust crate (termpane)"
kind = "rust"
root = "."
watch = ["**/*.rs", ".cargo/**", "Cargo.lock", "Cargo.toml", "benches/**", "examples/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml", "src/**", "tests/**"]
pr_commands = ["mbx fmt --manifest-path 'Cargo.toml' -- --check", "mbx clippy --locked --profile test --all-targets --all-features --package 'termpane' -- -D warnings", "mbx nextest run --locked --all-features --package 'termpane' --no-tests pass", "mbx test --locked --doc --all-features --package 'termpane'"]
full_commands = ["mbx fmt --manifest-path 'Cargo.toml' -- --check", "mbx clippy --locked --profile test --all-targets --all-features --package 'termpane' -- -D warnings", "mbx nextest run --locked --all-features --package 'termpane' --no-tests pass", "mbx test --locked --doc --all-features --package 'termpane'"]
phases = ["fmt", "clippy", "test", "doctest"]
check_commands = ["mbx check --locked --profile test --all-targets --all-features --package 'termpane'"]
platform = "linux-x64"
trust = "untrusted-ok"
[unit.capabilities]
docker = true
nested_privileged_docker = false
buildx_compose = false
testcontainers = true
services_with_readiness = false
browser_binaries = false
native_macos_arm64 = false
[unit.cache]
key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]

[[unit]]
id = "rust-termpane-fuzz"
label = "Rust crate (termpane-fuzz)"
kind = "rust"
root = "fuzz"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "fuzz/**/*.rs", "fuzz/Cargo.lock", "fuzz/Cargo.toml", "fuzz/benches/**", "fuzz/examples/**", "fuzz/src/**", "fuzz/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
pr_commands = ["cd -- 'fuzz' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'fuzz' && mbx clippy --locked --profile test --all-targets --all-features --package 'termpane-fuzz' -- -D warnings", "cd -- 'fuzz' && mbx nextest run --locked --all-features --package 'termpane-fuzz' --no-tests pass"]
full_commands = ["cd -- 'fuzz' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'fuzz' && mbx clippy --locked --profile test --all-targets --all-features --package 'termpane-fuzz' -- -D warnings", "cd -- 'fuzz' && mbx nextest run --locked --all-features --package 'termpane-fuzz' --no-tests pass"]
phases = ["fmt", "clippy", "test"]
check_commands = ["cd -- 'fuzz' && mbx check --locked --profile test --all-targets --all-features --package 'termpane-fuzz'"]
depends_on = ["rust-termpane"]
platform = "linux-x64"
trust = "untrusted-ok"
[unit.capabilities]
docker = true
nested_privileged_docker = false
buildx_compose = false
testcontainers = true
services_with_readiness = false
browser_binaries = false
native_macos_arm64 = false
[unit.cache]
key_files = [".cargo/**", "fuzz/Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]
